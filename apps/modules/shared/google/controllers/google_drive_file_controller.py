class GoogleDriveFileController:
    """Google Drive File Controller."""

    async def show(
        self,
        file_id: str,
    ) -> dict[str, str]:
        """Show the Google Drive file."""

        return {}
